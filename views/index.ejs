<!----<!DOCTYPE html>
<html>
<head>
  <title>Quotes from toscrape.com</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
    }
    h1 {
      color: #333;
      text-align: center;
    }
    table {
      border-collapse: collapse;
      width: 80%;
      margin: 20px auto;
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    th, td {
      padding: 12px 15px;
      border-bottom: 1px solid #ddd;
    }
    th {
      background-color: #f2f2f2;
      color: #333;
      text-transform: uppercase;
      font-size: 14px;
      letter-spacing: 1px;
      font-weight: 600;
    }
    td {
      color: #666;
    }
    tr:nth-child(even) {
      background-color: #f9f9f9;
    }
    tr:hover {
      background-color: #f2f2f2;
    }
    
  </style>
</head>
<body>
  <h1>Quotes from toscrape.com:</h1>
  <table>
    <thead>
      <tr>
        <th>Quote</th>
        <th>Author</th>
      </tr>
    </thead>
    <tbody>
      <% quotes.forEach((quote) => { %>
        <tr>
          <td><%= quote.quote %></td>
          <td><%= quote.author %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html>
---->
<!-- index.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quotes</title>
</head>
<body>
    <h1>Quotes</h1>
    <ul>
        <% quotes.forEach(quote => { %>
            <li><strong><%= quote.author %>:</strong> <%= quote.text %></li>
        <% }) %>
    </ul>
</body>
</html>

